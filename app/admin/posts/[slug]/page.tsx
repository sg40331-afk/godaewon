import Link from "next/link";import{ArrowLeft}from"lucide-react";import{requireAdmin}from"@/app/admin-auth";import{getPost}from"@/app/blog-store";import{PostEditor}from"../../post-editor";
export const dynamic="force-dynamic";export default async function EditPost({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const{allowed}=await requireAdmin(`/admin/posts/${slug}`);if(!allowed)return null;const post=await getPost(slug);if(!post)return <main className="admin-page"><div className="admin-shell"><h1>글을 찾을 수 없습니다.</h1><Link href="/admin">목록으로</Link></div></main>;return <main className="admin-page"><div className="admin-shell editor-shell"><Link href="/admin" className="admin-back"><ArrowLeft/>글 목록</Link><div className="editor-heading"><span>EDIT ARTICLE</span><h1>블로그 글 수정</h1></div><PostEditor initial={post}/></div></main>}
