"use client";
import{FormEvent,useState}from"react";
export default function AdminLogin(){const[password,setPassword]=useState("");const[message,setMessage]=useState("");const[busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setBusy(true);setMessage("");const res=await fetch("/api/admin/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({password})});const data=await res.json() as{error?:string};setBusy(false);if(!res.ok){setMessage(data.error||"로그인하지 못했습니다.");return}window.location.href="/admin"}return <main className="admin-login"><form onSubmit={submit}><img src="/daewon-tax-logo.png" alt="세무회계 대원"/><span>ADMIN</span><h1>블로그 관리자</h1><p>관리자 비밀번호를 입력해 주세요.</p><label>비밀번호<input type="password" value={password} onChange={e=>setPassword(e.target.value)} autoComplete="current-password" autoFocus/></label>{message&&<strong>{message}</strong>}<button disabled={busy}>{busy?"확인 중...":"로그인"}</button><a href="/">홈페이지로 돌아가기</a></form></main>}
